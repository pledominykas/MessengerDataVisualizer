<UserControl x:Class="MessengerDataVisualizer.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerDataVisualizer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#f1f1f1">
    <Border>
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="#2195f2" Padding="10,6" CornerRadius="5,5,0,0">
                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Bold" Foreground="White"/>
            </Border>
            <ScrollViewer DockPanel.Dock="Bottom">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="2,3">
                                <TextBlock Text="{Binding TimeSent}" FontSize="12" HorizontalAlignment="{Binding Alignment}" Foreground="#606060"/>
                                <Border Background="{Binding Color}" CornerRadius="3" Padding="5" HorizontalAlignment="{Binding Alignment}">
                                    <TextBlock TextWrapping="WrapWithOverflow" Text="{Binding Content}" Foreground="White"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
